-- Generated by ChatGPT

-- 1. Create APP_PENDING_USER table with the same structure as APP_USER (without data) and reconstruct constraints
CREATE TABLE APP_PENDING_USER AS
SELECT * FROM APP_USER WHERE 1 = 0;

ALTER TABLE APP_PENDING_USER DROP COLUMN ID;
ALTER TABLE APP_PENDING_USER DROP COLUMN USERNAME;
ALTER TABLE APP_PENDING_USER DROP COLUMN FIRST_NAME;
ALTER TABLE APP_PENDING_USER DROP COLUMN LAST_NAME;
ALTER TABLE APP_PENDING_USER DROP COLUMN IS_CLIENT;
ALTER TABLE APP_PENDING_USER DROP COLUMN IS_EMPLOYEE;
ALTER TABLE APP_PENDING_USER DROP COLUMN CREATED_AT;
ALTER TABLE APP_PENDING_USER DROP COLUMN UPDATED_AT;

ALTER TABLE APP_PENDING_USER
  ADD (
    ID NUMBER GENERATED BY DEFAULT ON NULL AS IDENTITY (START WITH 1 INCREMENT BY 1) NOT NULL,
    USERNAME VARCHAR2(100) NOT NULL,
    FIRST_NAME VARCHAR2(100) NOT NULL,
    LAST_NAME VARCHAR2(100) NOT NULL,
    IS_CLIENT   BOOLEAN DEFAULT FALSE NOT NULL,
    IS_EMPLOYEE BOOLEAN DEFAULT FALSE NOT NULL,
    CREATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL,
    UPDATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL
  );

-- 2. Add new columns
ALTER TABLE APP_PENDING_USER
  ADD (CLIENT_REGISTRATION_CODE   VARCHAR2(16),
       EMPLOYEE_REGISTRATION_CODE VARCHAR2(16));

-- 3. Recreate constraints

-- Primary Key Constraint
ALTER TABLE APP_PENDING_USER
  ADD CONSTRAINT pending_user_pk PRIMARY KEY (ID);

-- Unique Constraints
ALTER TABLE APP_PENDING_USER
  ADD CONSTRAINT pending_user_uuid_uk UNIQUE (UUID);

ALTER TABLE APP_PENDING_USER
  ADD CONSTRAINT pending_user_username_uk UNIQUE (USERNAME);

ALTER TABLE APP_PENDING_USER
  ADD CONSTRAINT pending_user_personal_email_uk UNIQUE (PERSONAL_EMAIL);

ALTER TABLE APP_PENDING_USER
  ADD CONSTRAINT pending_user_employee_email_uk UNIQUE (EMPLOYEE_EMAIL);

ALTER TABLE APP_PENDING_USER
  ADD CONSTRAINT pending_user_employee_id_uk UNIQUE (EMPLOYEE_ID);

-- Domain Logic Check Constraint
ALTER TABLE APP_PENDING_USER
  ADD CONSTRAINT pending_user_domain_logic_check CHECK (
    (NOT IS_CLIENT OR (PERSONAL_EMAIL IS NOT NULL AND CLIENT_PASSWORD_HASH IS NOT NULL))
    AND (NOT IS_EMPLOYEE OR (EMPLOYEE_ID IS NOT NULL AND EMPLOYEE_EMAIL IS NOT NULL AND EMPLOYEE_PASSWORD_HASH IS NOT NULL))
    AND (IS_CLIENT OR IS_EMPLOYEE)
  );

-- Non-empty string validations
ALTER TABLE APP_PENDING_USER
  ADD CONSTRAINT pending_user_non_empty_username CHECK (
    TRIM(USERNAME) IS NOT NULL AND TRIM(USERNAME) <> ''
  );

ALTER TABLE APP_PENDING_USER
  ADD CONSTRAINT pending_user_non_empty_firstname CHECK (
    TRIM(FIRST_NAME) IS NOT NULL AND TRIM(FIRST_NAME) <> ''
  );

ALTER TABLE APP_PENDING_USER
  ADD CONSTRAINT pending_user_non_empty_lastname CHECK (
    TRIM(LAST_NAME) IS NOT NULL AND TRIM(LAST_NAME) <> ''
  );

ALTER TABLE APP_PENDING_USER
  ADD CONSTRAINT pending_user_non_empty_personal_email CHECK (
    PERSONAL_EMAIL IS NULL OR TRIM(PERSONAL_EMAIL) <> ''
  );

ALTER TABLE APP_PENDING_USER
  ADD CONSTRAINT pending_user_non_empty_employee_email CHECK (
    EMPLOYEE_EMAIL IS NULL OR TRIM(EMPLOYEE_EMAIL) <> ''
  );

ALTER TABLE APP_PENDING_USER
  ADD CONSTRAINT pending_user_non_empty_employee_id CHECK (
    EMPLOYEE_ID IS NULL OR TRIM(EMPLOYEE_ID) <> ''
  );

-- Email format validation
ALTER TABLE APP_PENDING_USER
  ADD CONSTRAINT pending_user_email_format CHECK (
    (PERSONAL_EMAIL IS NULL OR
     REGEXP_LIKE(PERSONAL_EMAIL, '^[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}$'))
    AND
    (EMPLOYEE_EMAIL IS NULL OR
     REGEXP_LIKE(EMPLOYEE_EMAIL, '^[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}$'))
  );